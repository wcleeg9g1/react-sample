#react study 시작 2026.01.08


🔧 기술 스택

# 프로젝트 기본 스택 
  코어
  - React 19.2.0 + TypeScript 5.9.3
  - Vite 7.2.4 (빌드 도구)
  - React Router v6 (클라이언트 라우팅)

  상태 관리

  - TanStack Query 5.90.12 (서버 상태)
  - Zustand 5.0.9 (클라이언트 상태)

  UI & 스타일

  - Tailwind CSS 4.1.18 + Figma Tokens
  - TanStack Table 8.21.3 (데이터 테이블)

  데이터 & 검증

  - Axios 1.13.2 (HTTP 클라이언트)
  - Zod 4.2.1 (런타임 검증)

  ---
  🎨 디자인 토큰 워크플로우

  Figma (디자인)
      ↓ Figma Tokens 플러그인 export
  tokens.json
      ↓ generate-css.ts 실행
  theme.css (@theme block)
      ↓ Tailwind CSS v4 적용
  클래스 유틸리티 (bg-primary-500, p-md 등)

  ---

🚀 주요 명령어

  npm install     # 의존성 설치
  npm run dev     # 개발 서버 시작 (포트 5173)
  npm run build   # 프로덕션 빌드
  npm run lint    # ESLint 실행
  npm run preview # 프로덕션 빌드 미리보기


🔧 프로젝트 설치 
# 의존성 패키지 설치 확인 
npm list --depth=0
npm list -D --depth=0 # 개발용 

# 프로젝트 생성 
> npm create vite@latest . -- --template react-ts

# 기본 의존성 설치
## npm install -D (개발용으로 설치시는 -D 옵션 줘야함 / 글로벌설치 -g 옵션) 
> npm install

# React Router v6 설치
npm install react-router-dom@6

# TanStack Query (서버 상태)
npm install @tanstack/react-query@5
npm install -D @tanstack/eslint-plugin-query #개발용 

#Zustand (클라이언트 상태)
npm install zustand@5

#HTTP / 검증 라이브러리
npm install axios@1
npm install zod@4 -> 이걸로 npm install zod 다시설치함 

#UI / 스타일 관련
npm install tailwindcss@4 @tailwindcss/vite
-> 설치후 vite.config.ts 에 플러그인 import 

#개발용 (CSS를 파싱 browserslist 기준으로 필요한 prefix만 자동 추가)
#브라우저마다 CSS 표준 지원 시점이 달라서 자동으로 호환 시켜줌
npm install -D autoprefixer

# UI 없는(headless) 고성능 테이블 로직 라이브러리
# npm install @tanstack/react-table@8
npm install @tanstack/react-table
-> 설치후 index.css 상단에 import  
    @import "tailwindcss";

#  타입정의 설치? 
npm install -D @types/node

# Figma Tokens 워크플로우용 준비 
#Figma Tokens → Tailwind theme.css 자동화 스크립트
npm install -D ts-node
-> 설치후 세팅 필요 

react-test/
 ├─ design-tokens/
 │   └─ tokens.json        ← Figma Tokens 결과물
 ├─ scripts/
 │   └─ generate-css.ts    ← 변환 스크립트
 ├─ src/
 │   └─ styles/
 │       ├─ theme.css      ← 자동 생성
 │       └─ index.css

-> 순서 
(1) tokens.json 준비
→ (2) generate-css.ts 작성
→ (3) 스크립트 실행
→ (4) theme.css 생성
→ (5) Tailwind에서 사용

(1)tokens.json 
src/design-tokens/tokens.json  생성 (수동생성함)
npm run tokens:build

(2) 작성 (파일추가)
generate-css.ts 가 에러나서 
# package.app.json 에서 설정추가 
generate-css.ts
    /* Path Aliases */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
      
    },
"exclude": ["src/shared/design-tokens/generate-css.ts"] /* wclee 추가 */
# package.note.json 설정변경 
 "include": ["vite.config.ts", "tailwind.config.ts", "src/shared/design-tokens/**/*"] /* wclee 추가 */
------------------------------------------------------------------------------------------------------
# 참고용 
#tsconfig가 여러 개인 이유
브라우저에서 도는 코드
Node.js에서 도는 코드
공통 규칙

## tsconfig.json (루트 / 공통 기준)
기본 규칙 정의
다른 tsconfig들의 부모
-> 여기에는 거의 아무 설정도 안 둡니다.

## tsconfig.app.json (React / 브라우저용)
React + Vite에서 돌아가는 프론트엔드 코드 전용
------------------------------------------------------------------------------------------------------
# vite.config.ts 추가 세팅 
경로 alias 설정 및 프록시 설정 필요시 


🔧 프로젝트 기본 구조 
# 🟩 신규추가 폴더 및 파일 
root/
  │
  ├── 📄 index.html                # HTML 진입점
  ├── 📄 package.json              # 의존성 및 스크립트
  ├── 📄 vite.config.ts 🟩          # Vite 설정 (포트: 5173, API 프록시)
  ├── 📄 tsconfig.json 🟩           # TypeScript 설정
  ├── 📄 tailwind.config.ts 🟩      # Tailwind CSS 설정
  ├── 📄 eslint.config.js 🟩        # ESLint 설정
  ├── 📄 CLAUDE.md 🟩               # Claude 지침서
  ├── 📄 README.md 🟩               # 프로젝트 문서
  │
  ├── 📂 public/                   # 정적 자산
  │   └── vite.svg
  │
  ├── 📂 docs/ 🟩                  # 문서
  │   └── FIGMA_WORKFLOW.md 🟩       # Figma 토큰 워크플로우 가이드
  │
  ├── 📂 dist/                     # 빌드 출력 디렉토리
  │
  └── 📂 src/                      # 소스 코드 (Feature-Sliced Design)
      │
      ├── 📄 main.tsx              # 애플리케이션 진입점
      ├── 📄 index.css 🟩           # 전역 스타일 + Tailwind
      │
      ├── 📂 app/ 🟩                # 🔴 애플리케이션 레이어
      │   ├── layouts 🟩 
      │   │   └──Default.tsx 🟩             
      │   ├── providers/ 🟩              # 프로바이더 조합
      │   │   ├── query-client.tsx 🟩    # TanStack Query 설정
      │   │   └── router.tsx 🟩          # RouterProvider
      │   ├── router/ 🟩
      │   │     └── index.tsx 🟩           # 라우트 정의 (lazy loading)
      │   └── index.tsx 🟩  
      │
      ├── 📂 entities/ 🟩            # 🔵 엔티티 레이어 (비즈니스 엔티티)
      │   └── member/ 🟩 ### *** 도메인 하나 추가시 폴더 추가 ****
      │       ├── api/ 🟩
      │       │   └── users.api.ts 🟩    # 회원 API 엔드포인트
      │       ├── model/ 🟩
      │       │   ├── queries.ts 🟩      # TanStack Query 훅
      │       │   └── types.ts 🟩        # User 인터페이스 (34개 속성)
      │       └── index.ts 🟩            # Public API
      │
      ├── 📂 features/ 🟩            # 🟢 기능 레이어 (사용자 시나리오)
      │     └── add-bookmark/ 🟩        # 북마크 추가 기능 (구조만 존재)
      │
      ├── 📂 pages/ 🟩               # 🟠 페이지 레이어 (라우트 컴포넌트)
      │   └── home/ 🟩  ### *** 도메인 하나 추가시 폴더 추가 ****
      │       ├── ui/ 🟩
      │       │   └── HomePage.tsx 🟩    # 홈 페이지
      │       └── index.ts 🟩            # Public API
      │
      ├── 📂 shared/ 🟩              # 🟣 공유 레이어 (재사용 인프라)
      │   ├── api/ 🟩                 # API 클라이언트
      │   │   ├── client.ts 🟩       # Axios 인스턴스 + 인터셉터
      │   │   └── index.ts 🟩
      │   ├── config/ 🟩              # 설정
      │   │   └── index.ts 🟩        # 앱 설정 객체
      │   ├── design-tokens/ 🟩       # 디자인 시스템
      │   │   ├── generate-css.ts 🟩  # 토큰→CSS 변환 스크립트
      │   │   ├── theme.css 🟩        # 생성된 CSS 테마
      │   │   └── tokens.json 🟩      # Figma 디자인 토큰
      │   ├── lib/ 🟩                 # 유틸리티
      │   │   ├── cn.ts 🟩           # 클래스명 결합 유틸
      │   │   └── index.ts 🟩
      │   └── ui/ 🟩                  # 공유 UI 컴포넌트
      │       ├── ErrorBoundary.tsx 🟩
      │       ├── LoadingFallback.tsx 🟩
      │       └── index.ts 🟩
      │
      ├── 📂 widgets/ 🟩             # 🟡 위젯 레이어 (복합 UI 블록)
          ├── bookmarks-table/ 🟩
          │   ├── ui/ 🟩
          │   │   └── header.tsx 🟩      # 테이블 헤더
          │   └── index.ts 🟩
          └── header/ 🟩
              ├── ui/ 🟩
              │   └── Header.tsx 🟩      # 네비게이션 헤더
              └── index.ts 🟩



 🏗️ Feature-Sliced Design 계층 구조

          ┌─────────────────────────┐
          │   app (최상위 레이어)     │  ← 앱 초기화, 프로바이더
          └─────────────────────────┘
                    ↓
          ┌─────────────────────────┐
          │   pages (페이지)         │  ← 라우트별 페이지
          └─────────────────────────┘
                    ↓
          ┌─────────────────────────┐
          │   widgets (위젯)         │  ← 복합 UI 블록
          └─────────────────────────┘
                    ↓
          ┌─────────────────────────┐
          │   features (기능)        │  ← 사용자 시나리오
          └─────────────────────────┘
                    ↓
          ┌─────────────────────────┐
          │   entities (엔티티)      │  ← 비즈니스 모델
          └─────────────────────────┘
                    ↓
          ┌─────────────────────────┐
          │   shared (공유)          │  ← 재사용 인프라
          └─────────────────────────┘

  임포트 규칙: 상위 레이어는 하위 레이어만 임포트 가능

  